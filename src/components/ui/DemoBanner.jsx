/**
 * DemoBanner — P2C.R4: neutralised. The legacy amber "UI / UX Prototype"
 * top bar previously rendered on every shell page in /clinic and /admin
 * has been removed so the workspace feels operational. The Local Review
 * Tools page (/design-preview/review-tools) is now the single visible
 * surface that talks about demo data and review datasets.
 */
export function DemoBanner() {
  return null
}
